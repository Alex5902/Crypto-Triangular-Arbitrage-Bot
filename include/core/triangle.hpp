#ifndef TRIANGLE_HPP
#define TRIANGLE_HPP

#include <string>
#include <vector>

// Put NOTHING else here: no references to Simulator or scanning
struct Triangle {
    std::string base;
    std::vector<std::string> path;
};

#endif // TRIANGLE_HPP
